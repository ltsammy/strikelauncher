<Window x:Class="StrikeLauncher.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Einstellungen - Strike Launcher"
        Icon="Assets/app.ico"
        Height="600" Width="640"
        Background="{StaticResource BackgroundBrush}"
        Foreground="{StaticResource TextBrush}"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="36" GlassFrameThickness="0" UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#0D1116" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource PanelBorderBrush}" />
            <Setter Property="BorderThickness" Value="1.5" />
            <Setter Property="Padding" Value="6,4" />
            <Setter Property="Margin" Value="0,2,0,10" />
            <Setter Property="CaretBrush" Value="{StaticResource AccentBrush}" />
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource MutedBrush}" />
            <Setter Property="FontSize" Value="12" />
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Margin" Value="0,4,0,10" />
        </Style>
    </Window.Resources>

    <Border BorderBrush="{StaticResource PanelBorderBrush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="36" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Custom title bar -->
            <Grid Grid.Row="0" Background="#0D1014">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0">
                    <Image Source="Assets/app.ico" Width="16" Height="16" />
                    <TextBlock Text="EINSTELLUNGEN" Margin="8,0,0,0" FontSize="11" FontWeight="SemiBold"
                               Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center" />
                </StackPanel>
                <Button Content="✕" Click="OnCloseIconClick" Style="{StaticResource TitleBarCloseButtonStyle}"
                        HorizontalAlignment="Right" shell:WindowChrome.IsHitTestVisibleInChrome="True" />
            </Grid>

            <Grid Grid.Row="1">
                <!-- Layer 0: same blurred/darkened background treatment as the main window -->
                <Grid ClipToBounds="True">
                    <Rectangle Fill="{StaticResource FallbackBackgroundBrush}" />
                    <Image x:Name="BackgroundImageElement" Stretch="UniformToFill" Effect="{StaticResource BackgroundBlurEffect}" />
                    <Rectangle Fill="{StaticResource DarkenOverlayBrush}" />
                </Grid>

                <!-- Layer 1: content -->
                <Border Margin="16" Style="{StaticResource GlassPanelStyle}" Padding="20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="VERBINDUNG &amp; PFADE" FontWeight="SemiBold" FontSize="14" Foreground="{StaticResource AccentBrush}" Margin="0,0,0,14" />

                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <TextBlock Text="Arma 3 Installationspfad" />
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBox x:Name="Arma3PathBox" Grid.Column="0" Margin="0,2,8,10" />
                                    <Button Grid.Column="1" Content="Durchsuchen" Style="{StaticResource GhostButtonStyle}" Click="OnBrowseArma3Click" />
                                </Grid>

                                <TextBlock Text="TeamSpeak 3 Installationspfad" />
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBox x:Name="TeamSpeakPathBox" Grid.Column="0" Margin="0,2,8,10" />
                                    <Button Grid.Column="1" Content="Durchsuchen" Style="{StaticResource GhostButtonStyle}" Click="OnBrowseTeamSpeakClick" />
                                </Grid>

                                <TextBlock Text="Spielername (Nickname)" />
                                <TextBox x:Name="NicknameBox" />

                                <TextBlock Text="Modlist-URL (modlist.html)" />
                                <TextBox x:Name="ModlistUrlBox" />

                                <TextBlock Text="Serverdaten-URL (launcher.json)" />
                                <TextBox x:Name="ServerDataUrlBox" />

                                <TextBlock Text="Task Force Radio Plugin-URL (.ts3_plugin)" />
                                <TextBox x:Name="Ts3PluginUrlBox" />

                                <TextBlock Text="GitHub Repo für Auto-Update (https://github.com/owner/repo)" />
                                <TextBox x:Name="GithubRepoUrlBox" />

                                <CheckBox x:Name="MuteSoundsCheck" Content="TeamSpeak Benachrichtigungstöne stummschalten" />
                            </StackPanel>
                        </ScrollViewer>

                        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
                            <Button Content="Abbrechen" Style="{StaticResource GhostButtonStyle}" Click="OnCancelClick" Margin="0,0,8,0" />
                            <Button Content="Speichern" Click="OnSaveClick" />
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
